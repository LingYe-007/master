\newpage
% =========================================================
% 第五章：基于联邦异构图模型的推荐系统设计与实现
% =========================================================

\section{第五章\texorpdfstring{\quad}{} 基于联邦异构图模型的推荐系统设计与实现}
\label{chap:system_design}
\setcounter{section}{5}
\setcounter{subsection}{0}

本章将第三章的 FedASCL 与第四章的语义感知压缩策略集成到一个完整的学术论文推荐系统中，用于应对传统集中式推荐中的隐私泄露风险以及落地中的数据异质性、通信瓶颈等工程问题。以下按系统需求分析、总体架构设计、关键模块实现与功能测试四部分展开。

% ----------------------------------------------------------------------
\subsection{系统需求分析}
\label{sec:sys_requirements}

本节从业务与用户两方面对联邦推荐系统做功能定义。

\subsubsection{业务需求分析}

系统需满足以下业务要求：支持用户注册与本地私有库，保证原始交互不出域，端云仅交换加密的梯度或原型以阻断隐私风险（联邦身份认证）；服务端下发全局原型与模型参数，客户端完成双视图对比学习与压缩上传，服务端加权聚合更新全局模型（端云协同训练）；集成本地差分隐私，对上传原型或梯度加噪以抵御恶意与推理攻击（隐私安全保障）；并采用动态元路径剪枝与残差梯度量化（第四章策略），在受限带宽下迭代训练（高效通信管理）。

上述业务功能需求的关系与数据流如图~\ref{fig:sys_arch} 所示（系统总体设计架构图见 5.2.1 节）。

\subsubsection{用户需求分析}

系统主要面向学术研究人员，功能上包括：个人画像管理，供用户维护研究兴趣、机构与方向等属性作为冷启动属性视图输入，数据仅存本地并以加密形式参与训练；联邦训练监控，为管理员提供 Loss、Hit@5、NDCG@5 等曲线与在线状态，支撑异常诊断与调度；学术资源推荐，依据是否有交互历史在“结构优先”与“属性优先”策略间切换，给出个性化论文列表并可结合元路径注意力做可解释展示。

系统用户需求与上述模块的对应关系已在 5.3.3 与 5.4 节的实现与测试中体现。

% ----------------------------------------------------------------------
\subsection{系统总体架构设计}
\label{sec:sys_architecture}

本系统采用分层架构设计，实现了算法逻辑与业务应用的高度解耦。

\subsubsection{逻辑架构设计}

系统自下而上划分为四层，如图~\ref{fig:sys_arch} 所示：底层数据存储层以客户端 SQLite（自我中心子图与用户属性）与服务端 MySQL/Redis（全局参数与元数据）分离落实不出域；其上算法引擎层封装 FedASCL（双视图对比、原型对齐）、语义感知压缩（元路径选择器、残差梯度量化）与 FedAvg 安全聚合；业务服务层提供推荐列表、冷启动与异步调度等 API；顶层业务展示层基于 React，支持文献浏览与训练状态可视化。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.78\textwidth,height=0.38\textheight,keepaspectratio]{images/system_architecture_cn.png}
    \caption{系统总体设计架构图}
    \label{fig:sys_arch}
\end{figure}

\subsubsection{数据不出域机制设计}

数据存储层采用客户端-服务端分离的架构。每个客户端仅存储以当前用户为中心的自我中心子图（Ego-Subgraph），包括用户节点及其直接交互的论文、作者等节点，原始图数据与敏感属性严格保留在本地。本地子图存储方式如图~\ref{fig:storage_layer} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth,height=0.36\textheight,keepaspectratio]{images/用户子图示意图.png}
    \caption{客户端用户子图存储示意图}
    \label{fig:storage_layer}
\end{figure}

% ----------------------------------------------------------------------
\subsection{系统关键模块实现}
\label{sec:sys_modules}

本节详细介绍系统关键功能的开发环境与实现逻辑。

\subsubsection{开发与运行环境}

系统基于 Python 生态构建，采用前后端分离模式。硬件环境采用 Nvidia GPU 算力平台；软件选型包括 Python 3.8、PyTorch、PyTorch Geometric（PyG）、Flask、Redis、Celery 等。技术选型见表~\ref{tab:tech_stack}。

\begin{table}[H]
    \centering
    \caption{系统技术选型与开发环境}
    \label{tab:tech_stack}
    \begin{tabular}{ll}
        \toprule
        组件类别 & 技术选型 \\
        \midrule
        开发语言 & Python 3.8 \\
        深度学习框架 & PyTorch 1.10, PyTorch Geometric (PyG) \\
        通信协议 & RESTful API / gRPC (模型参数传输) \\
        后端框架 & Flask (API服务), Celery (异步任务调度) \\
        数据存储 & MySQL 8.0 (元数据), Redis 6.0 (热数据) \\
        前端框架 & React + TypeScript + ECharts (可视化) \\
        \bottomrule
    \end{tabular}
\end{table}

\subsubsection{联邦协同训练模块实现}

该模块协调分布式客户端与中心服务器进行多轮联邦学习，实现模型分发（广播）、本地更新、压缩与上传、安全聚合四个阶段的闭环。
\begin{cnenumerate}
    \cnitem 同步联邦训练流程。单轮训练包含以下步骤，即模型分发（广播）——服务端随机选择 $K$ 个客户端，下发全局模型参数 $W_g$ 与全局属性原型 $P_g$；本地更新——客户端基于本地异构子图执行多轮 FedASCL 训练，双视图编码器分别提取结构特征与属性特征，通过最大化互信息优化节点表示；压缩与上传——训练结束后，客户端经语义感知压缩器处理梯度，仅上传压缩后的梯度更新与本地聚类中心，原始图数据保留在本地；安全聚合——服务端收集各节点更新，执行加权聚合，更新全局模型版本。
    \cnitem 仿真引擎与分级推荐路由。系统根据用户是否具备交互历史，在常规推荐（结构视图单通路）与冷启动推荐（FedASCL 属性—结构双视图）之间进行路由，总体流程如图~\ref{fig:simulator_arch} 所示。工程上，交互历史充分的用户优先采用与 FedHGNN 同类异构图结构编码一致的轻量前向路径，以降低在线推断时双编码与对比模块的算力开销；联邦训练与全局收敛仍以 FedASCL 双视图及原型对齐为主，与第三章实验设定一致，二者体现推断路径与训练目标的取舍而非推翻算法对比结论。为验证算法可行性，后端同时实现高保真仿真引擎：\texttt{Data\allowbreak Partitioner} 将 ACM 等数据集切分为多个互不重叠的 Ego-Subgraph，模拟 Non-IID 分布；多线程机制模拟客户端并发训练与网络延迟。
\end{cnenumerate}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth,height=0.36\textheight,keepaspectratio]{images/联邦学习仿真引擎架构图.png}
    \caption{联邦学习仿真引擎与学术资源分级推荐流程示意图}
    \label{fig:simulator_arch}
\end{figure}

\subsubsection{学术资源推荐与冷启动模块实现}

该模块基于训练好的联邦异构图模型为用户提供个性化学术资源推荐，并重点支持冷启动场景。系统按交互历史路由不同推理分支（图~\ref{fig:rec_logic}）：活跃用户走结构优先路径，聚合“用户-论文-会议”等高阶邻域生成嵌入并检索 Top-N 文献；新用户无交互时启用 FedASCL 属性分支，将兴趣、机构等映射至全局原型空间再做相似检索，实现零样本冷启动。同时以元路径注意力权重增强可解释性，例如 \texttt{User-\allowbreak Author-\allowbreak User} 权重高时，可提示“推荐该学者是因为你们有共同的合作者”。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.82\textwidth,height=0.36\textheight,keepaspectratio]{images/冷启动推荐策略流程图.png}
    \caption{基于属性-结构双通路的冷启动推荐策略流程图}
    \label{fig:rec_logic}
\end{figure}

\subsubsection{通信压缩与隐私模块实现}

客户端上传阶段集成第四章的语义感知压缩（动态元路径选择器与残差梯度量化），逻辑同 4.2--4.3 节，在约 $20\times$ 压缩比下兼顾精度与通信开销；同时对上传的全局原型或梯度施加拉普拉斯噪声（Laplace Mechanism），通过配置隐私预算 $\varepsilon$ 与敏感度，在 $\varepsilon$-差分隐私（$\delta=0$）意义上抑制标签推理与后门风险，并尽量控制对收敛的影响。

% ----------------------------------------------------------------------
\subsection{系统功能测试}
\label{sec:sys_test}

通过黑盒测试验证系统在给定业务场景下的稳定性与可行性。

\subsubsection{主要功能测试}

本节对系统做黑盒功能测试：事先定好用例、操作步骤和预期现象，实验中按步骤执行并把是否满足预期记入表中结论栏。各模块的检查内容、预期与结论见表~\ref{tab:func_test}；下面用文字按行说明实际怎么做、凭什么记为「通过」。

\begin{table}[H]
    \centering
    \caption{系统主要功能测试用例及结果}
    \label{tab:func_test}
    % 表格内 m 列段落：略收紧 baselineskip，并限制 lineskip，避免含上标公式时行间空隙过大
    \renewcommand{\arraystretch}{1.08}
    \begin{tabular}{>{\centering\arraybackslash}m{2.4cm}>{\setlength{\baselineskip}{13pt}\setlength{\lineskiplimit}{0pt}\setlength{\lineskip}{0.5pt}\raggedright\arraybackslash}m{5.2cm}>{\setlength{\baselineskip}{13pt}\setlength{\lineskiplimit}{0pt}\setlength{\lineskip}{0.5pt}\raggedright\arraybackslash}m{5.0cm}>{\centering\arraybackslash}m{1.2cm}}
        \toprule
        测试模块 & 测试用例描述 & 预期结果 & 结论 \\
        \midrule
        \multirow{2}{*}{联邦控制} & 验证全局模型参数的版本迭代 & 轮次 $t{+}1$ 的参数与 $t$ 轮\mbox{发生更新} & 通过 \\
        \cmidrule(lr){2-4}
         & 验证数据隔离性 & 服务端无法读取客户端的原始图结构 & 通过 \\
        \midrule
        \multirow{2}{*}{推荐服务} & 历史交互丰富的老用户请求推荐 & 推荐列表与历史偏好领域一致性高 & 通过 \\
        \cmidrule(lr){2-4}
         & 仅有属性信息的冷启动用户请求推荐 & 系统基于属性原型返回相关领域文献 & 通过 \\
        \midrule
        系统监控 & 前端实时拉取训练 Loss、Hit@5、NDCG@5 等指标 & 曲线随训练轮次动态更新且趋势收敛 & 通过 \\
        \bottomrule
    \end{tabular}
\end{table}

表~\ref{tab:func_test} 各行的核验要点可概括为：联邦控制侧在相邻轮次间对全局参数作差应有非零更新，并在服务器侧核对未出现各端本地子图邻接与原始属性的泄露；推荐服务侧分别以交互充足与仅含属性的账号对照返回列表与画像；监控侧观察 Loss、Hit@5、NDCG@5 曲线是否随轮次刷新并呈合理收敛形态。上述检查的界面层佐证见图~\ref{fig:ui_rec}、图~\ref{fig:ui_settings} 与图~\ref{fig:ui_monitor}。

% 三张界面截图统一版心宽度与最大高度；题注仅保留简短题名，功能说明放在图后正文段，避免「图题下一长句」挤在题注里
\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth,height=0.34\textheight,keepaspectratio]{images/论文推荐页面.png}
    \caption{学术资源推荐列表界面}
    \label{fig:ui_rec}
\end{figure}

图~\ref{fig:ui_rec}面向「学术资源推荐」业务：主区展示按模型排序的论文列表（题名、作者、出版物与摘要），便于在线浏览与比对；右侧「元路径说明」给出当前推荐所依赖的异质图路径（如 $U{-}P{-}A{-}P$、$U{-}P{-}K{-}P$ 等形式化记号及自然语言解释），与 5.2.1 节业务展示层及「结构优先/属性优先」路由一致，符号 $U$、$P$、$A$、$V$、$K$、$I$ 等与第三章 FedASCL 元路径语义一致，用于把高阶邻域语义转为终端可读的可解释推荐依据。

\vspace{-0.2\baselineskip}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth,height=0.34\textheight,keepaspectratio]{images/个人设置页面.png}
    \caption{个人画像与账户设置界面}
    \label{fig:ui_settings}
\end{figure}

图~\ref{fig:ui_settings}用于个人画像与账户管理：上部以只读方式展示系统生成的用户标识与登录名等账户信息，便于与实验台账或学号规则对齐；下部表单维护个人编号、姓名（可选）、机构、研究兴趣与研究方向等字段，为冷启动场景构造属性视图输入。界面提示强调画像与敏感属性缓存在本地，与 5.2.2 节数据不出域及端侧存储边界一致，对应表~\ref{tab:func_test} 中与冷启动、画像相关的测试场景。

\vspace{-0.2\baselineskip}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.86\textwidth,height=0.34\textheight,keepaspectratio]{images/中央服务器训练面板.png}
    \caption{联邦训练状态监控界面}
    \label{fig:ui_monitor}
\end{figure}

图~\ref{fig:ui_monitor}面向管理员或实验人员的训练监控：汇总当前训练范式、任务状态与轮次进度；「训练控制面板」集中展示副服务器数、总轮次、本地 epoch、批大小、学习率、设备与客户端规模等超参；折线图实时绘制全局 Loss 及 Hit@5、NDCG@5 随通信轮次的变化；底部日志记录模型分发、客户端上报、安全聚合、原型同步与语义压缩等事件。该视图对应 5.3.2 节「模型分发—本地更新—压缩与上传—安全聚合」闭环的运行态，并与表~\ref{tab:func_test} 中系统监控类用例及第三、四章训练与压缩流程叙述一致。

\subsubsection{性能与鲁棒性验证}

除上述界面层验证外，以下从通信与挂钟时间补充系统级性能数据。

通信开销对比。在相同数据集与训练轮次下，对比语义感知压缩开关时的累计流量；仿真采用第四章一致配置（$\rho=0.2$、$b=4$，约 $20\times$ 压缩比），100 轮、每轮 10 客户端场景下，全精度上传累计约 12.2 MB，开启压缩后约 0.62 MB，压缩比约 $20\times$，与表~\ref{tab:communication_cost} 量级一致；挂钟时间由约 45 分钟降至约 6 分钟，加速约 $7.5\times$，表明集成后通信开销可降低约一个数量级，与第四章结论一致。

测试结果表明，系统成功实现了联邦学习的完整闭环，具备模型分发、本地训练、梯度聚合及基于模型的在线推荐能力；在隐私保护与通信效率方面，数据隔离与语义感知压缩均达到设计目标，验证了 FedASCL 框架在工程落地上的可行性。

% ----------------------------------------------------------------------
\subsection{本章小结}
\label{sec:sys_summary}

本章阐述了基于 FedASCL 与语义感知压缩策略的联邦推荐系统的设计与实现。在需求分析部分，明确了联邦身份认证、端云协同训练、隐私保障与高效通信等业务需求，以及个人画像管理、训练监控与学术推荐等用户需求。在架构设计部分，给出了自下而上的四层逻辑架构与数据不出域机制。在关键实现部分，从开发环境、联邦协同训练、学术资源推荐与冷启动、通信压缩与隐私等模块做了介绍。功能测试与性能鲁棒性验证（含系统级通信开销与训练耗时实测数据）表明，系统在功能完备性、推荐准确性、隐私保护与通信效率上达到设计目标，完成了从算法到系统的完整链路，可在学术推荐场景下实际部署。
