\newpage
\section{第三章\texorpdfstring{\quad}{} 基于属性-结构双视图对比学习的联邦异构图推荐算法}
\label{chap:fedascl}
\setcounter{section}{3} \setcounter{subsection}{0}

第二章介绍了推荐系统、图神经网络、联邦学习及对比学习等理论基础。本章针对第一章提出的冷启动与数据异质性问题，给出 FedASCL 框架，先说明问题背景与动机，再介绍算法总体框架及各核心模块，最后通过实验验证有效性。

\subsection{问题说明}
\label{sec:motivation}
在联邦推荐系统的实际应用场景中，数据稀疏性与冷启动问题构成了限制模型性能的关键瓶颈。在联邦生态扩展背景下，冷启动的维度已从传统的“新用户与新物品”层级演变为更为棘手的“新客户端”层级\cite{zhang2024ifedrec}。

传统的联邦推荐算法主要依赖用户-物品交互图进行特征提取。然而，在新用户、新物品或新客户端接入初期，由于缺乏充分的历史交互记录，基于元路径的图卷积网络（GCN）往往难以捕捉有意义的节点结构表征，导致推荐性能明显下降。与此相对的是，用户属性（如年龄、职业）和物品属性（如类别、描述文本）通常包含丰富的语义信息，且在冷启动阶段即可在客户端本地获取。若能利用这些属性信息，将为缓解交互稀疏性提供重要补充\cite{sang2025hgcl}。

此外，联邦学习固有的数据非独立同分布特性极易导致不同客户端之间出现模型参数的语义漂移，加剧了冷启动场景下的推荐偏差\cite{tan2023fedstar}。

基于上述挑战，本章提出基于属性-结构双视图对比学习的联邦异构图推荐算法（Federated Attribute-Structure Contrastive Learning, FedASCL），即通过双视图对比学习将属性语义迁移至交互空间，并引入基于原型的语义对齐以缓解 Non-IID 带来的分布偏差，在保护隐私的前提下提升冷启动场景下的推荐质量\cite{wang2025fedpcl}。

\subsection{算法总体框架} 
\label{sec:framework}
FedASCL 采用常见的服务器-客户端联邦训练方式，其端到端执行流程包含以下六个步骤。
\begin{cnenumerate}
    \cnitem 客户端本地双视图构建。客户端首先基于用户和物品的原始属性构建属性语义图，同时利用本地交互历史构建交互结构图，形成互补的双视图输入。
    \cnitem 双视图特征编码。利用异构图神经网络分别对结构视图和属性视图进行编码，学习节点在不同视角下的高维特征表示。
    \cnitem 细粒度跨视图对比。在客户端本地执行跨视图对比学习，用于最大化属性语义表示与交互结构表示之间的互信息，完成语义迁移与增强\cite{crad2025multiview}。
    \cnitem 原型语义对齐。服务器聚合生成全局语义原型并下发，客户端利用这些原型对本地节点表示进行正则化约束，以缓解 Non-IID 导致的模型偏差；与通过全局知识引导融合本地嵌入的联邦推荐框架在跨客户端对齐目标上相通\cite{lim2025fedrkg}。
    \cnitem 模型聚合与更新。服务器收集各客户端上传的模型参数（或梯度），采用加权平均策略聚合后更新全局模型。
    \cnitem 联合优化与推荐。客户端利用预定义的元路径挖掘异构图中的高阶语义关联，结合推荐主任务损失与对比学习辅助损失进行联合反向传播，完成模型参数的闭环更新。
\end{cnenumerate}

本文 FedASCL 联邦训练总体流程如下图~\ref{fig:fedascl_framework} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.48\textheight,keepaspectratio]{images/fedascl_framework.png}
    \caption{FedASCL算法总体架构示意图}
    \label{fig:fedascl_framework}
\end{figure}

\subsection{属性语义图构建}
\label{subsec:attr_sem_graph}

为弥补冷启动场景下交互结构的缺失，本章利用 $k$-近邻算法基于原始属性特征构建属性语义超图。该过程用于挖掘节点间潜在的语义关联，构建出显式的图结构供图神经网络学习\cite{sang2025hgcl}。具体构建流程形式化描述如下。

\subsubsection{特征预处理与向量化}
设联邦系统中的用户集合为 $\mathcal{U} = \{u_1, u_2, \dots, u_N\}$，物品集合为 $\mathcal{I} = \{i_1, i_2, \dots, i_M\}$。对于任意节点 $v \in \mathcal{U} \cup \mathcal{I}$，其原始属性集合包含离散型特征（如性别、类别）和连续型特征（如年龄、价格）。

为了将异构属性映射到统一的特征空间，首先对离散特征采用独热（One-hot）编码，对连续特征采用 Min-Max 归一化处理。假设节点 $v$ 处理后的特征向量为 $\mathbf{x}_v \in \mathbb{R}^{d_{attr}}$，其计算过程表示为：
\begin{equation}
    \mathbf{x}_v = \text{Concat}\left( \text{OneHot}(\mathbf{f}_{cat}), \frac{\mathbf{f}_{cont} - \min(\mathbf{f}_{cont})}{\max(\mathbf{f}_{cont}) - \min(\mathbf{f}_{cont})} \right)
\end{equation}
其中，$\mathbf{f}_{cat}$ 和 $\mathbf{f}_{cont}$ 分别表示原始的类别特征和连续特征向量，$\text{Concat}(\cdot)$ 为向量拼接操作。

\subsubsection{属性语义相似度计算}
在获得标准化的属性特征向量后，为了衡量节点间在语义层面的相似程度，本章采用余弦相似度作为度量标准。对于任意两个节点 $v_i$ 和 $v_j$，其语义相似度 $S_{ij}$ 定义为：
\begin{equation}
    S_{ij} = \cos(\mathbf{x}_i, \mathbf{x}_j) = \frac{\mathbf{x}_i^\top \mathbf{x}_j}{\|\mathbf{x}_i\|_2 \cdot \|\mathbf{x}_j\|_2}
\end{equation}
需要注意的是，在联邦学习场景下，为了保护隐私，上述计算通常限制在同类型节点之间（即用户-用户、物品-物品），或者在客户端本地利用部分脱敏的公共属性进行计算。

\subsubsection{kNN语义超图构建}
基于计算得到的相似度矩阵 $\mathbf{S}$，我们利用 $k$-近邻算法构建超图结构。超图 $\mathcal{G} = (\mathcal{V}, \mathcal{E})$ 由节点集合 $\mathcal{V}$ 和超边集合 $\mathcal{E}$ 组成，其中每条超边 $e \in \mathcal{E}$ 可以连接任意数量的节点，能够捕捉高阶语义关联。

对于每个节点 $v_i$，我们选取与其相似度最高的 $k$ 个节点构成邻居集合 $\mathcal{N}_k(v_i)$：
\begin{equation}
    \mathcal{N}_k(v_i) = \{ v_j \mid S_{ij} \in \text{Top-}k(S_{i \cdot}) \}
\end{equation}

为了构建超图，本章采用"节点即超边"的策略，即每个节点 $v_i$ 生成一条以自身为中心的超边 $e_i$，该超边包含节点 $v_i$ 及其所有 $k$ 个近邻。超图的结构通过关联矩阵 $\mathbf{H} \in \mathbb{R}^{|\mathcal{V}| \times |\mathcal{E}|}$ 来进行数学表达，其中 $|\mathcal{E}| = |\mathcal{V}|$。关联矩阵中的元素 $H_{ve}$ 定义如下：
\begin{equation}
    H_{ve} = \begin{cases} 
    1, & \text{if } v \in e \\
    0, & \text{otherwise}
    \end{cases}
\end{equation}
如果节点 $v_j$ 属于节点 $v_i$ 的 $k$ 近邻集合（即 $v_j \in \mathcal{N}_k(v_i) \cup \{v_i\}$），则在关联矩阵中 $H_{j, e_i} = 1$。

通过上述方式构建的属性语义超图关联矩阵 $\mathbf{H}$，将作为后续属性视图图神经网络的输入，用于聚合高阶语义特征。相较于普通图邻接矩阵，$\mathbf{H}$ 能够更灵活地建模"具有相似属性的一组节点"之间的群组关系，从而在冷启动阶段提供鲁棒的特征表示。

本文属性语义图构建流程如下图~\ref{fig:attr_semantic_build} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.46\textheight,keepaspectratio]{images/属性语义图构建.png}
    \caption{属性语义图构建流程示意图}
    \label{fig:attr_semantic_build}
\end{figure}

\subsection{跨视图对比学习机制}
\label{subsec:cross_view_cl}
为实现从属性域到交互域的知识迁移，本章设计了细粒度的跨视图对比学习机制，即基于互信息最大化，使同一节点在“属性视图”与“结构视图”下的表示在共享嵌入空间中保持语义一致\cite{wang2025fedpcl}，包含双视图编码与对比优化两个阶段。

\subsubsection{双视图特征编码}
设 $\mathcal{V}$ 为节点集合（包含用户与物品）。为了捕获不同视角的语义，我们采用两个独立的异构图神经网络（HGNN）作为编码器。
\begin{cnenumerate}
    \cnitem {\songti 结构视图编码。}基于用户-物品交互图 $\mathcal{G}_{stru}$，利用异构图卷积层聚合邻居信息。设 $\mathbf{h}_i^{(l)}$ 为节点 $i$ 在第 $l$ 层的隐藏状态，其更新规则为：
    \begin{equation}
        \mathbf{z}_i^{stru} = \text{HGNN}_{stru}(\mathcal{G}_{stru}, \mathbf{X})
    \end{equation}
    其中 $\mathbf{z}_i^{stru}$ 是节点 $i$ 经过多层聚合后的结构及其上下文特征表示，$\mathbf{X}$ 为初始特征矩阵。结构视图侧重于捕获"协同过滤"信号。
    \cnitem 属性视图编码。基于第\ref{subsec:attr_sem_graph}节构建的属性语义超图 $\mathcal{G}_{attr}$（对应关联矩阵 $\mathbf{H}$），采用超图卷积网络进行编码：
    \begin{equation}
        \mathbf{z}_i^{attr} = \text{HGNN}_{attr}(\mathbf{H}, \mathbf{X}) = \sigma(\mathbf{D}_v^{-1/2} \mathbf{H} \mathbf{W}_e \mathbf{D}_e^{-1} \mathbf{H}^\top \mathbf{D}_v^{-1/2} \mathbf{X} \mathbf{\Theta})
    \end{equation}
    其中 $\mathbf{z}_i^{attr}$ 是节点 $i$ 的属性语义表示。该视图侧重于捕获"内容相似性"信号，对于冷启动节点而言，该向量是主要的推断依据。
\end{cnenumerate}



\subsubsection{InfoNCE 损失函数推导}
为了对齐 $\mathbf{z}_i^{stru}$ 和 $\mathbf{z}_i^{attr}$，我们将同一节点在两个视图下的嵌入视为{\songti 正样本对}，将该节点与其他节点在任意视图下的嵌入视为{\songti 负样本对}。

首先，定义两个向量 $\mathbf{u}, \mathbf{v}$ 之间的相似度度量函数 $\text{sim}(\cdot, \cdot)$，本文采用余弦相似度：
\begin{equation}
    \text{sim}(\mathbf{u}, \mathbf{v}) = \frac{\mathbf{u}^\top \mathbf{v}}{\|\mathbf{u}\| \cdot \|\mathbf{v}\|}
\end{equation}

我们采用 InfoNCE 损失函数来最大化正样本对之间的互信息下界。对于一个包含 $N$ 个节点的训练批次，节点 $i$ 的跨视图对比损失 $\mathcal{L}_{cl}^{(i)}$ 定义为：

\begin{equation}
    \mathcal{L}_{cl}^{(i)} = - \log \frac{\exp(\text{sim}(\mathbf{z}_i^{stru}, \mathbf{z}_i^{attr}) / \tau)}{\sum_{j=1, j \neq i}^{N} \underbrace{\exp(\text{sim}(\mathbf{z}_i^{stru}, \mathbf{z}_j^{attr}) / \tau)}_{\text{Inter-view Negatives}} + \sum_{j=1, j \neq i}^{N} \underbrace{\exp(\text{sim}(\mathbf{z}_i^{stru}, \mathbf{z}_j^{stru}) / \tau)}_{\text{Intra-view Negatives}}}
    \label{eq:cl_loss}
\end{equation}
为了简化计算，在实际部署中我们主要关注跨视图负样本（即分母的第一部分）。该损失函数通过拉近分子中的正样本对、推远分母中的负样本对，实现了特征空间的对齐\cite{wu2021sgl, yu2022simgcl}。

\subsubsection{温度系数 \texorpdfstring{$\tau$}{tau} 的梯度传播分析}
公式中的 $\tau$ 是一个关键的超参数。虽然它仅是一个标量，但对梯度的传播机制和语义迁移效果有着决定性影响。

我们可以通过分析损失函数对负样本相似度 $s_{i,j} = \text{sim}(\mathbf{z}_i^{stru}, \mathbf{z}_j^{attr})$ 的梯度来理解 $\tau$ 的作用。根据链式法则，梯度 $\frac{\partial \mathcal{L}_{cl}}{\partial s_{i,j}}$ 为：
\begin{equation}
    \frac{\partial \mathcal{L}_{cl}}{\partial s_{i,j}} = \frac{1}{\tau} \cdot \frac{\exp(s_{i,j}/\tau)}{\sum_{k \neq i} \exp(s_{i,k}/\tau)} = \frac{1}{\tau} \cdot P_{i,j}
\end{equation}
其中 $P_{i,j}$ 可以理解为模型将负样本 $j$ 误判为正样本的相对概率（即负样本的"困难程度"）。当 $\tau \to 0$（极小值）时，分布变得极度尖锐，$P_{i,j}$ 仅在那些与锚点相似度极高（$s_{i,j}$ 很大）的负样本上非零，而对容易区分的负样本几乎为 0，模型因而专注于困难负样本并迫使属性视图学习更细粒度区分，但也可能带来难收敛或数值不稳定；当 $\tau \to \infty$（极大值）时，分布趋于均匀，各负样本权重 $P_{i,j}$ 近乎相等，梯度对样本区分力减弱，难以获得高质量语义表征。

因此，$\tau$ 在本质上控制了模型对困难负样本的关注程度。在联邦推荐的冷启动场景中，我们需要适中的 $\tau$（如实验中设为 0.2），既能利用属性区分相似物品，又避免因过度关注噪声数据而导致模型过拟合。

本文跨视图对比学习机制如下图~\ref{fig:cross_view_cl} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.44\textheight,keepaspectratio]{images/跨视图对比机制.png}
    \caption{跨视图对比学习机制示意图}
    \label{fig:cross_view_cl}
\end{figure}

\subsection{基于原型的语义对齐策略}
\label{subsec:prototype_alignment}
在联邦推荐场景下，不同客户端的数据分布往往存在明显的异构性。例如，某些客户端可能仅包含"科幻类"电影的交互记录，而另一部分客户端则集中于"爱情类"电影。这种局部数据的偏斜会导致本地训练得到的节点表示在嵌入空间中发生语义漂移，即同一类别的物品在不同客户端的嵌入表示大相径庭，严重阻碍了全局模型的收敛与泛化\cite{tan2022fedproto}。

针对该问题，本章引入全局语义原型机制。原型定义为嵌入空间中某类语义特征的聚类中心；服务器聚合全局原型并下发，客户端用这些“全局锚点”对本地表示做正则化约束，实现语义对齐\cite{lim2025fedrkg}。

\subsubsection{本地原型计算与上传}
在每一轮联邦训练的本地更新阶段结束后，客户端 $m$ 基于当前的节点嵌入表示计算本地语义原型。假设系统中设定了 $K$ 个潜在的语义类别（例如对应 $K$ 个物品类别或通过聚类得到的 $K$ 个簇）。

对于第 $k$ 个语义类别，客户端 $m$ 计算属于该类别的所有节点嵌入的均值作为本地原型 $\mathbf{c}_{m,k}^{(t)}$：
\begin{equation}
    \mathbf{c}_{m,k}^{(t)} = \frac{1}{|\mathcal{S}_{m,k}|} \sum_{i \in \mathcal{S}_{m,k}} \mathbf{z}_i^{(t)}
\end{equation}
其中，$\mathcal{S}_{m,k}$ 表示客户端 $m$ 中属于第 $k$ 类语义的节点集合（例如交互过的某类物品集合），$\mathbf{z}_i^{(t)}$ 为节点 $i$ 在第 $t$ 轮的双视图融合嵌入。

为了保护隐私，客户端不直接上传原始嵌入 $\mathbf{z}_i$，而是仅上传聚合后的原型 $\mathbf{c}_{m,k}^{(t)}$。此外，为满足差分隐私要求，我们在上传前对原型添加拉普拉斯噪声或高斯噪声 $\xi$：
\begin{equation}
    \tilde{\mathbf{c}}_{m,k}^{(t)} = \mathbf{c}_{m,k}^{(t)} + \xi, \quad \xi \sim \text{Laplace}(0, \frac{\Delta}{\varepsilon})
\end{equation}

\subsubsection{全局原型聚合}
服务器收到各参与客户端上传的带噪本地原型后，执行加权聚合操作，生成全局语义原型 $\mathbf{C}^{(t)} = \{ \mathbf{c}_1^{(t)}, \dots, \mathbf{c}_K^{(t)} \}$。

第 $k$ 个全局原型的计算公式为：
\begin{equation}
    \mathbf{c}_k^{(t)} = \frac{\sum_{m \in \mathcal{M}^{(t)}} |\mathcal{S}_{m,k}| \cdot \tilde{\mathbf{c}}_{m,k}^{(t)}}{\sum_{m \in \mathcal{M}^{(t)}} |\mathcal{S}_{m,k}|}
\end{equation}
其中 $\mathcal{M}^{(t)}$ 是当前轮次参与训练的客户端集合。全局原型代表了该类语义在所有客户端视角下的“平均”或“公允”表示，随后被广播回所有客户端。该加权形式与 FedProto 等工作中常见的样本量加权叙述一致，实现开销低；但当各客户端 $|\mathcal{S}_{m,k}|$ 差异极大时，少数大客户端仍可能对 $\mathbf{c}_k^{(t)}$ 施加过大影响，极端 Non-IID 下可进一步考虑中位数型聚合、信赖域加权或稳健统计等变体，本文正文仍以上式样本量加权形式为主干，以便与 FedProto 等基线叙述对齐。

\subsubsection{本地语义对齐损失}
客户端接收到全局原型 $\mathbf{C}^{(t)}$ 后，将其作为正则化约束引入本地训练。我们的目标是让本地节点的嵌入 $\mathbf{z}_i$ 尽可能靠近其所属类别的全局原型 $\mathbf{c}_{type(i)}$，同时远离其他类别的原型 $\mathbf{c}_{j} (j \neq type(i))$。

因此，我们设计了基于原型的对比对齐损失（Prototype-based Contrastive Loss）：
\begin{equation}
    \mathcal{L}_{proto} = \sum_{i \in \mathcal{B}} - \log \frac{\exp(\mathbf{z}_i \cdot \mathbf{c}_{type(i)}^{(t)} / \tau_p)}{\sum_{k=1}^K \exp(\mathbf{z}_i \cdot \mathbf{c}_k^{(t)} / \tau_p)}
    \label{eq:proto_loss}
\end{equation}
其中，$\mathcal{B}$ 为本地训练批次，$type(i)$ 指示节点 $i$ 所属的语义类别，$\tau_p$ 为原型对比的温度系数。

该策略的作用主要体现在两个方面：纠偏层面上，即便本地数据分布极度倾斜（例如全是动作片），全局原型仍能指示其他语义（如"爱情片"）在嵌入空间中的大致位置，从而防止本地模型为拟合局部数据而破坏整体语义结构；冷启动增强层面上，新接入客户端往往缺乏交互、初始嵌入随机或由属性粗估，全局原型为其提供高质量参照，使其快速对齐全局分布。

本文基于原型的语义对齐流程如下图~\ref{fig:prototype_alignment} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth,height=0.44\textheight,keepaspectratio]{images/基于原型的语义对齐策略.png}
    \caption{基于原型的语义对齐策略示意图}
    \label{fig:prototype_alignment}
\end{figure}

\subsection{基于元路径的推荐与联合优化}
\label{subsec:metapath_rec}

在通过双视图编码与对比学习获得高质量的节点表示后，FedASCL 算法利用元路径（Meta-path）机制来捕捉异构图中隐含的高阶语义关联，并执行最终的推荐任务。

\subsubsection{元路径定义与语义提取}
元路径 $\Phi$ 定义为连接网络模式中节点类型的复合关系序列，即 $A_1 \xrightarrow{R_1} A_2 \xrightarrow{R_2} \dots \xrightarrow{R_l} A_{l+1}$。在推荐场景中，不同的元路径代表了不同的语义偏好。本章主要考虑两类元路径并组成集合 $\mathcal{P} = \{\Phi_1, \Phi_2, \dots, \Phi_P\}$：协同关联路径（如 U-I-U，刻画购买过相同物品的用户之相似偏好）与属性关联路径（如 U-A-U、I-C-I，借助属性图语义连接弥补交互稀疏\cite{li2023metapath}）。

对于特定的元路径 $\Phi_p$，我们采用路径特定的 GCN 聚合该路径下的邻居信息，得到节点 $i$ 在该路径下的语义特定嵌入 $\mathbf{z}_i^{\Phi_p}$。

\subsubsection{语义级注意力融合机制}
由于不同元路径对用户意图的贡献度不同（例如，对于冷启动用户，属性路径 U-A-U 的重要性可能高于协同路径 U-I-U），简单的平均融合无法适应复杂的联邦场景。因此，我们设计了语义级注意力机制（Semantic-level Attention）来自适应地学习各元路径的权重\cite{wang2019han}。

首先，将各元路径下的节点嵌入 $\mathbf{z}_i^{\Phi_p}$ 输入到一个非线性变换中，计算其重要性分数：
\begin{equation}
    w_{\Phi_p} = \frac{1}{|\mathcal{V}|} \sum_{i \in \mathcal{V}} \mathbf{q}^\top \cdot \tanh(\mathbf{W}_{att} \mathbf{z}_i^{\Phi_p} + \mathbf{b}_{att})
\end{equation}
其中，$\mathbf{W}_{att}$ 和 $\mathbf{b}_{att}$ 是可学习的权重矩阵和偏置向量，$\mathbf{q}$ 是语义级别的注意力向量。

随后，利用 Softmax 函数对重要性分数进行归一化，得到最终的元路径权重 $\beta_{\Phi_p}$：
\begin{equation}
    \beta_{\Phi_p} = \frac{\exp(w_{\Phi_p})}{\sum_{p'=1}^P \exp(w_{\Phi_{p'}})}
\end{equation}

最后，基于学习到的权重对不同元路径的嵌入进行加权求和，得到融合了多重语义的最终节点表示 $\mathbf{z}_i^*$：
\begin{equation}
    \mathbf{z}_i^* = \sum_{p=1}^P \beta_{\Phi_p} \cdot \mathbf{z}_i^{\Phi_p}
\end{equation}
该机制使得模型能够根据数据特性自动调整对结构信息与属性信息的依赖程度。

\subsubsection{评分预测与主任务损失}
基于融合后的用户最终表示 $\mathbf{z}_u^*$ 和物品最终表示 $\mathbf{z}_i^*$，我们采用内积操作预测用户 $u$ 对物品 $i$ 的偏好评分 $\hat{y}_{ui}$：
\begin{equation}
    \hat{y}_{ui} = (\mathbf{z}_u^*)^\top \mathbf{z}_i^*
\end{equation}

为了优化推荐排序性能，本章采用贝叶斯个性化排序损失作为主任务损失函数。BPR 假设观察到的交互（正样本）评分应高于未观察到的交互（负样本）。对于训练集中的三元组 $(u, i, j)$，其中 $i$ 为正样本物品，$j$ 为负样本物品，主任务损失定义为：
\begin{equation}
    \mathcal{L}_{rec} = \sum_{(u,i,j) \in \mathcal{D}} - \ln \sigma(\hat{y}_{ui} - \hat{y}_{uj}) + \lambda \|\Theta\|_2^2
\end{equation}
其中 $\sigma(\cdot)$ 是 Sigmoid 函数，$\Theta$ 表示模型参数，最后一项为 $L_2$ 正则化项。

\subsubsection{多任务联合优化}
为了在联邦训练中同时兼顾推荐准确性、冷启动适应性与 Non-IID 鲁棒性，我们将前文提出的跨视图对比损失 $\mathcal{L}_{cl}$（公式 \ref{eq:cl_loss}）和原型对齐损失 $\mathcal{L}_{proto}$（公式 \ref{eq:proto_loss}）作为辅助任务，构建最终的联合优化目标函数：
\begin{equation}
    \mathcal{L}_{total} = \mathcal{L}_{rec} + \lambda_1 \mathcal{L}_{cl} + \lambda_2 \mathcal{L}_{proto}
\end{equation}
其中，$\lambda_1$ 和 $\lambda_2$ 是用于平衡不同任务权重的超参数。在客户端本地训练过程中，通过反向传播算法最小化 $\mathcal{L}_{total}$ 来更新本地模型参数，随后上传至服务器进行聚合。

\subsection{算法分析}
\label{sec:algorithm_analysis}

\subsubsection{算法过程}
\label{subsec:algorithm_process}
结合第~\ref{subsec:attr_sem_graph} 节至第~\ref{subsec:metapath_rec} 节给出的属性语义图、跨视图对比、原型对齐以及元路径推荐与联合损失，可将一次联邦迭代概括为相互衔接的若干阶段，以便与算法~\ref{alg:fedasclrc} 中的记号对齐。
\begin{cnenumerate}
    \cnitem 初始化与首轮构图。服务器初始化全局参数 $\Theta^{(0)}$ 与全局原型 $\mathbf{C}^{(0)}$；各客户端在 $t=1$ 时依据第~\ref{subsec:attr_sem_graph} 节构造属性语义超图 $\mathcal{G}_{attr}$，该结构在后续轮次中复用，仅在属性或近邻策略变更时重建。
    \cnitem 本地前向与损失计算。对每个活跃客户端，在结构图 $\mathcal{G}_{stru}$ 与 $\mathcal{G}_{attr}$ 上分别计算 $\mathbf{Z}^{stru}$、$\mathbf{Z}^{attr}$，按式~\eqref{eq:cl_loss}、式~\eqref{eq:proto_loss} 与 BPR 主任务项组成 $\mathcal{L}_{total}$，并在本地执行 $E$ 个小轮次的梯度下降。
    \cnitem 原型上传与隐私处理。客户端在本地聚类得到原型后按前述拉普拉斯（或高斯）加噪机制得到 $\tilde{\mathbf{c}}_{m,k}^{(t)}$，与更新后的模型参数一并上传，避免回传原始交互或逐点嵌入。
    \cnitem 服务器聚合与广播。服务器按样本量加权聚合模型参数，并按第~\ref{subsec:prototype_alignment} 节所述规则更新 $\mathbf{C}^{(t)}$，再将新的全局参数与原型下发至下一轮被选中的客户端。
\end{cnenumerate}
以下算法~\ref{alg:fedasclrc} 在上述顺序基础上，对多轮外层循环与多客户端并行过程采用合并写法列出，各伪代码行与上文公式及小节标题的对应关系仍保持不变。

% 用mdframed包裹algorithm，添加黑色边框（需要安装 mdframed 包）
% 暂时注释掉 mdframed，使用普通边框
\begin{algorithm}[H]  % H 表示强制在当前位置，不浮动
    \caption{FedASCL 联邦训练算法过程}
    \label{alg:fedasclrc}
    \begingroup
    \small
    \setlength{\baselineskip}{0.78\baselineskip}  % 收紧行距，使算法过程在一页内
    \setlength{\lineskip}{0.5pt}
    \begin{algorithmic}[1] % [1]表示每行编号
        \Require 用户集合 $\mathcal{U}$，物品集合 $\mathcal{I}$，属性特征 $\mathbf{X}$；\\
                交互图 $\mathcal{G}_{stru}$；\\
                联邦全局轮次 $T$，本地训练轮次 $E$，学习率 $\eta$；\\
            超参数：近邻数 $k$，温度系数 $\tau, \tau_p$，损失权重 $\lambda_1, \lambda_2$。
        \Ensure 训练好的全局模型参数 $\Theta^*$。

        % 服务器初始化
        \State Server Initialization: 初始化全局模型参数 $\Theta^{(0)}$ 和全局语义原型 $\mathbf{C}^{(0)}$。

        % 阶段一：联邦训练循环

        \For{$t = 1, 2, \dots, T$}
            \State 服务器随机选取活跃客户端集合 $\mathcal{M}^{(t)}$；
            \State 服务器下发全局参数 $\Theta^{(t-1)}$ 和全局原型 $\mathbf{C}^{(t-1)}$ 至客户端；
            
            % 客户端并行训练
            \For{each client $m \in \mathcal{M}^{(t)}$}
     
              
                \If{$t=1$} 
                    \State 视图构建 (\text{Sec 3.3}): 基于公式 (\text{3.1})-(\text{3.4})，利用kNN构建属性语义超图 $\mathcal{G}_{attr}$（仅首次训练执行）；
                \EndIf
                \State 加载全局参数 $\Theta_m \leftarrow \Theta^{(t-1)}$；
                
                % 本地训练轮次
                \For{$e = 1, \dots, E$}
                    \State 特征编码 (\text{Sec 3.4.1}): 
                    \State \qquad 计算结构视图表示 $\mathbf{Z}^{stru} \leftarrow \text{HGNN}_{stru}(\mathcal{G}_{stru}, \mathbf{X})$；
                    \State \qquad 计算属性视图表示 $\mathbf{Z}^{attr} \leftarrow \text{HGNN}_{attr}(\mathcal{G}_{attr}, \mathbf{X})$；
                    
                    \State 损失计算 (\text{Sec 3.6.4}):
                    \State \qquad 跨视图对比损失 $\mathcal{L}_{cl}$（\text{Eq. 3.8}）；
                    \State \qquad 原型对齐损失 $\mathcal{L}_{proto}$（\text{Eq. 3.13}）；
                    \State \qquad 推荐主任务损失 $\mathcal{L}_{rec}$（\text{Eq. 3.18}）；
                    \State \qquad 总损失 $\mathcal{L}_{total} = \mathcal{L}_{rec} + \lambda_1 \mathcal{L}_{cl} + \lambda_2 \mathcal{L}_{proto}$；
                    
                    \State 参数更新: $\Theta_m \leftarrow \Theta_m - \eta \nabla \mathcal{L}_{total}$；
                \EndFor
                
                \State 原型计算 (\text{Sec 3.5.1}): 计算本地原型 $\mathbf{c}_{m}^{(t)}$，并添加隐私噪声以满足差分隐私（\text{Eq. 3.10-3.11}）；
                \State 上传本地模型 $\Theta_m$ 和加噪后的本地原型 $\tilde{\mathbf{c}}_{m}^{(t)}$ 至服务器；
            \EndFor

            % 服务器端聚合
            \State 模型聚合: $\Theta^{(t)} \leftarrow \sum_{m \in \mathcal{M}^{(t)}} \frac{|\mathcal{D}_m|}{|\mathcal{D}|} \Theta_m$（按客户端数据量加权平均）；
            \State 原型聚合: 基于加权平均更新全局语义原型 $\mathbf{C}^{(t)}$（\text{Eq. 3.12}）；
        \EndFor
        \State \Return 最终全局模型参数 $\Theta^{(T)}$（即 $\Theta^*$）。
    \end{algorithmic}
    \endgroup
\end{algorithm}

从执行顺序看，算法~\ref{alg:fedasclrc} 在每轮联邦迭代中依次完成参数下发、各活跃客户端的本地多轮更新以及服务器端的模型与原型聚合：本地阶段在首轮构建属性语义超图后固定复用，其后各轮重复编码、损失计算与梯度更新；上传内容包含更新后的本地参数与加噪原型，服务器据此刷新全局模型与全局原型并进入下一轮。伪代码中标注的节号与公式编号与正文一致，便于在实现中将各伪代码步骤映射到具体函数模块。

\subsubsection{复杂度分析}
\label{subsec:fedascl_complexity}

该过程与前文各小节中的符号及公式定义一致，其计算与存储开销分析如下。

本节从时间复杂度和空间复杂度两个维度对 FedASCL 算法进行理论分析。在此，设系统中节点总数为 $N$（包括用户和物品），交互边总数为 $M$。设特征嵌入维度为 $d$，GCN 与 HGNN 的网络层数为 $L$，训练时的批次大小为 $B$。FedASCL 的时间开销主要由三个阶段组成，即属性图构建、双视图特征编码与联合优化。

在预处理阶段，我们需要基于 $k$-近邻算法构建属性语义超图：对 $N$ 个节点暴力计算两两余弦相似度并排序的复杂度为 $O(N^2 d + N^2 \log N)$，但在联邦场景下常在本地子集 $n_m$（$n_m \ll N$）上执行或借助 LSH 等近似近邻加速，实际可降至 $O(N \cdot k \cdot d)$；随后选取 Top-$k$ 邻居生成关联矩阵 $\mathbf{H}$ 为线性操作，复杂度 $O(N \cdot k)$。
需要注意的是，该过程通常作为一次性离线计算完成，不计入每轮训练的实时开销中。

在每一轮训练的前向传播中，时间开销主要来自图卷积：结构视图在用户–物品二部图 $\mathcal{G}_{stru}$ 上做消息传递，稀疏实现下单层与边数线性相关即 $O(M \cdot d)$，$L$ 层合计 $O(L \cdot M \cdot d)$；属性视图基于超图关联矩阵 $\mathbf{H}$，每节点连 $k$ 个邻居使非零元约为 $N(k+1)$，$\mathbf{H}\mathbf{H}^\top \mathbf{X}$ 一类操作主导复杂度约 $O(L \cdot N \cdot k \cdot d)$。
由于 $k$ 通常较小（实验中设为 5--10），且 $M \gg N$，属性视图的引入并未明显增加图传播的数量级。

联合优化阶段，跨视图对比损失 $\mathcal{L}_{cl}$ 若在全图计算 InfoNCE 为 $O(N^2 d)$，不可行；本章采用批内训练策略，仅在批量 $B$ 内计算相似度，降为 $O(B^2 d)$，且 $B \ll N$ 时开销很低。原型对齐损失 $\mathcal{L}_{proto}$ 需与 $K$ 个全局原型比对，为 $O(B \cdot K \cdot d)$（$K$ 为常数级语义类数）。主任务 BPR 损失 $\mathcal{L}_{rec}$ 仅涉正负样本对内积，为 $O(B \cdot d)$。

因此，FedASCL 单次迭代的总时间复杂度为：
\begin{equation}
    T_{total} \approx O\Big( L(M + Nk)d + B^2d + BKd \Big)
\end{equation}
由于 $M$ 通常远大于 $Nk$ 和 $B^2$，算法的主导复杂度仍为 $O(LMd)$，这与标准的 LightGCN 等基准模型保持一致\cite{he2020lightgcn}。这表明 FedASCL 在引入对比学习增强语义表示的同时，未引入过高的计算负担。在空间开销方面，主要涉及模型参数、图结构数据以及中间嵌入表示的存储。

\begin{cnenumerate}
    \cnitem 图结构存储。我们采用压缩稀疏行（CSR）格式存储邻接矩阵。
    \begin{subenumerate}
        \item 结构图 $\mathcal{G}_{stru}$ 需要存储 $2M$ 条有向边，空间复杂度为 $O(M)$。
        \item 属性超图 $\mathcal{G}_{attr}$ 的关联矩阵 $\mathbf{H}$ 包含 $N(k+1)$ 个非零项，空间复杂度为 $O(Nk)$。
    \end{subenumerate}
    \cnitem 模型参数与嵌入。
    \begin{subenumerate}
        \item 节点嵌入。存储所有用户和物品的 $d$ 维嵌入向量，空间开销为 $O(N \cdot d)$。这是推荐系统内存占用的主要部分。
        \item 网络权重。GCN 和 HGNN 的变换矩阵通常较小（如 $d \times d$），空间复杂度为 $O(L \cdot d^2)$。
        \item 全局原型。存储 $K$ 个全局原型向量，开销为 $O(K \cdot d)$，相对于节点嵌入可忽略不计。
    \end{subenumerate}
\end{cnenumerate}

综上，FedASCL 的总空间复杂度为 $O(M + Nk + Nd)$。该空间占用与节点数和边数呈线性关系，证明了模型在大规模稀疏数据集上的可扩展性。

\subsection{实验设置}
\label{subsec:exp_setup}

为验证 FedASCL 框架的可行性，本章在三个公开数据集上做实验，围绕以下三个问题展开。
\begin{enumerate}[label=\arabic*., labelsep=0.08em, leftmargin=2.1em, labelwidth=1.65em, align=left, noitemsep, topsep=2pt, parsep=0pt]
    \item RQ1（总体性能）FedASCL 在常规联邦推荐场景下，与现有强基线相比总体推荐表现如何？
    \item RQ2（冷启动适应性）在缺乏交互历史的冷启动场景下，双视图机制能否提升推荐质量？
    \item RQ3（鲁棒性与消融）面对 Non-IID 数据分布，算法是否具有鲁棒性？各关键模块（对比学习、原型对齐）贡献如何？
\end{enumerate}

\subsubsection{数据集与预处理}
实验选取了三个具有丰富属性信息的代表性数据集，即 MovieLens-1M、Yelp 和 ACM，具体统计信息见表~\ref{tab:dataset_stat}。上述公开基准用于验证 FedASCL 的通用性。

\begin{table}[H]
    \centering
    \caption{实验数据集统计信息}
    \label{tab:dataset_stat}
    \begin{tabular}{lccccc}
        \toprule
        Dataset & 用户侧节点 & \#\,Users & 物品侧节点 & \#\,Items & 稀疏度 \\
        \midrule
        MovieLens-1M & 注册用户 & 6,040 & 电影 & 3,706 & 95.53\% \\
        Yelp & 评论用户 & 15,480 & 商户 & 12,095 & 99.66\% \\
        ACM & 作者 & 25,678 & 论文 & 18,432 & 99.82\% \\
        \bottomrule
    \end{tabular}
\end{table}

表~\ref{tab:dataset_stat} 给出三个数据集的规模与稀疏度，其中用户侧、物品侧列说明各领域实体在推荐二部图中的角色（MovieLens 为注册用户与电影，Yelp 为评论用户与商户，ACM 为作者与论文）。

\begin{table}[H]
    \centering
    \caption{MovieLens-1M 数据集属性字段及编码方式}
    \label{tab:dataset_attr_ml}
    \begin{tabular}{lllll}
        \toprule
        节点类型 & 属性名称 & 属性字段 & 字段类型 & 编码方式 \\
        \midrule
        用户 & 性别 & Gender & 离散 & One-hot \\
        用户 & 年龄 & Age & 离散 & One-hot \\
        用户 & 职业 & Occupation & 离散 & One-hot \\
        用户 & 邮政编码 & Zip-code & 离散 & One-hot \\
        物品 & 影片类型 & Genres & 离散多标签 & Multi-hot \\
        物品 & 片名 & Title & 文本 & 文本 \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Yelp 数据集属性字段及编码方式}
    \label{tab:dataset_attr_yelp}
    \begin{tabular}{lllll}
        \toprule
        节点类型 & 属性名称 & 属性字段 & 字段类型 & 编码方式 \\
        \midrule
        用户 & 平均评分 & average\_stars & 连续 & Min-Max \\
        用户 & 评论数 & review\_count & 连续 & Min-Max \\
        用户 & 有用投票 & useful & 连续 & Min-Max \\
        用户 & 有趣投票 & funny & 连续 & Min-Max \\
        用户 & 酷投票 & cool & 连续 & Min-Max \\
        用户 & 粉丝数 & fans & 连续 & Min-Max \\
        用户 & 精英标记 & elite & 离散多标签 & One-hot \\
        物品 & 类别 & categories & 离散多标签 & Multi-hot \\
        物品 & 星级 & stars & 连续 & Min-Max \\
        物品 & 评论数 & review\_count & 连续 & Min-Max \\
        物品 & 城市 & city & 离散 & One-hot \\
        物品 & 州、省 & state & 离散 & One-hot \\
        物品 & 纬度 & latitude & 连续 & Min-Max \\
        物品 & 经度 & longitude & 连续 & Min-Max \\
        物品 & 是否营业 & is\_open & 离散 & One-hot \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{ACM 数据集属性字段及编码方式}
    \label{tab:dataset_attr_acm}
    \begin{tabular}{lllll}
        \toprule
        节点类型 & 属性名称 & 属性字段 & 字段类型 & 编码方式 \\
        \midrule
        用户 & 作者编号 & author\_id & 离散 & 索引编码 \\
        用户 & 姓名 & name & 文本 & 文本 \\
        用户 & 所属机构 & affiliation & 离散 & One-hot \\
        用户 & 研究兴趣 & interests & 离散多标签 & Multi-hot \\
        用户 & 研究方向 & research\_direction & 离散多标签 & Multi-hot \\
        用户 & 关键词 & keywords & 离散多标签 & Multi-hot \\
        用户 & 学科领域 & subject & 离散 & One-hot \\
        物品 & 论文标题 & title & 文本 & 文本 \\
        物品 & 发表会议/期刊 & venue & 离散 & One-hot \\
        物品 & 发表年份 & year & 连续 & Min-Max \\
        物品 & 摘要 & abstract & 文本 & 文本 \\
        物品 & 关键词 & keywords & 离散多标签 & Multi-hot \\
        物品 & 学科领域 & subject & 离散 & One-hot \\
        \bottomrule
    \end{tabular}
\end{table}

表~\ref{tab:dataset_attr_ml}、表~\ref{tab:dataset_attr_yelp} 与表~\ref{tab:dataset_attr_acm} 的节点类型列统一为用户与物品两类。属性名称列为中文语义，属性字段列为数据源中的原始列名或经规则派生的语义字段。各表用于构造第~\ref{subsec:attr_sem_graph} 节属性向量 $\mathbf{x}_v$。其中，ACM 作者侧字段与第五章系统「个人设置」页中的个人编号（对应 \texttt{author\_id}）、姓名、所属机构、研究兴趣与研究方向一一对应；论文侧字段与「学术推荐」列表中的标题、作者（经 U--P 交互边与 U--P--A--P 元路径建模）、发表会议/期刊、年份、摘要及关键词相对应，便于离线实验与原型系统演示共用同一套属性语义。离散多标签字段（如 \texttt{interests}、\texttt{keywords}）经词表映射后 Multi-hot 编码；文本字段（\texttt{name}、\texttt{title}、\texttt{abstract}）在构图阶段可经预训练词向量或 TF-IDF 降维后并入 $\mathbf{x}_v$，与 MovieLens 片名字段处理方式一致。由于交互密度、任务语义与划分方式并不相同，同一算法在不同数据集上的绝对指标不宜直接横向比较数值大小（例如 ACM 作者与论文二部图的 Hit@10 与 MovieLens 评分矩阵不具备可比性），更合理的做法是对照各基线的相对提升以及跨数据集上的趋势是否一致。其中，MovieLens-1M 交互相对稠密、稀疏度最低，可视为中等难度基准。Yelp 与 ACM 的稀疏度分别达到 99.66\% 与 99.82\%，正负样本不均衡、有效监督更稀缺，对模型在弱观测条件下的泛化能力要求更高。

为了模拟联邦学习环境，我们利用狄利克雷分布 $\text{Dir}(\alpha)$ 将交互数据划分到 $N_{client}=50$ 个客户端中。其中 $\alpha$ 控制数据异构程度，$\alpha$ 越小表示 Non-IID 程度越高。除非特殊说明，默认设置 $\alpha=1.0$。

\subsubsection{对比基线模型}
本实验选取三类代表性联邦推荐算法作为对比基准：FedGNN\cite{wu2021fedgnn} 作为联邦图神经网络代表，FedHGNN\cite{yan2024fedhgnn} 覆盖异构图联邦建模，FedProto\cite{tan2022fedproto} 代表联邦原型对齐路线。三者分别对应同构 GNN、异构 HGNN 与原型正则三类主流范式，与本文方法在总体性能、冷启动与 Non-IID 子实验中形成对照。

此外，联邦对比学习与原型增强（如 FedPCL\cite{wang2025fedpcl}）、面向冷启动迁移的原型类工作（如 FRCSU\cite{li2025frcsu}、FedDMR\cite{feddmr2025} 等）与本文在图编码器结构、双视图与元路径推荐头的联合训练管线上仍不完全同构。评测采用 Hit@K 与 NDCG@K 两项排序指标，与上述工作亦不完全一致。若一并纳入同一张主性能表，易混淆实现细节与公平性假设。故本章数值对比仍以表~\ref{tab:overall_performance} 所列主干基线为限，上述路线在绪论与相关工作中从问题设定与优化目标上对照讨论。

\subsubsection{参数设置}
评估指标采用 Hit@K 与 NDCG@K（$K=10, 20$；Hit@K 按第二章式~\eqref{eq:hit} 计算，表示前 $K$ 个推荐中是否至少命中一个正样本；NDCG@K 为前 $K$ 位的归一化折损累积增益）。关键超参数设置如下。属性近邻数 $k=10$，对比学习温度 $\tau=0.2$，原型温度 $\tau_p=0.5$，损失权重 $\lambda_1=\lambda_2=0.1$；语义原型类别数 $K$ 与物品类别（或聚类簇）数一致，用于式~\eqref{eq:proto_loss} 中的 $type(i)$。每轮随机采样 20\% 的客户端参与训练。

\subsubsection{实验环境与配置汇总}
实验在配备 Nvidia GPU 的服务器上运行，软件环境为 Python 3.8、PyTorch 1.10、PyTorch Geometric (PyG)。为便于复现，固定随机种子（如 \texttt{seed=42}），联邦训练总轮数 $T=100$，客户端本地每轮训练轮次 $E=5$，学习率 $\eta=0.001$，优化器采用 Adam。上述参数与数据集、基线及指标设置一并汇总于表~\ref{tab:exp_config_chap3}，便于与第四章、第五章的实验与系统配置对照。

\begin{table}[H]
    \centering
    \caption{第三章实验配置汇总}
    \label{tab:exp_config_chap3}
    \begin{tabular}{ll}
        \toprule
        项目 & 配置 \\
        \midrule
        运行环境 & Python 3.8, PyTorch 1.10, PyG, Nvidia GPU \\
        数据集 & MovieLens-1M, Yelp, ACM（见表~\ref{tab:dataset_stat}） \\
        联邦划分 & $N_{client}=50$，$\text{Dir}(\alpha)$，默认 $\alpha=1.0$ \\
        每轮参与率 & 20\% 客户端随机采样 \\
        训练轮数 & 全局 $T=100$，本地每轮 $E=5$ \\
        优化器 & Adam，学习率 $\eta=0.001$ \\
        超参数 & $k=10$，$\tau=0.2$，$\tau_p=0.5$，$\lambda_1=\lambda_2=0.1$ \\
        评估指标 & Hit@10 与 Hit@20，NDCG@10 与 NDCG@20 \\
        随机种子 & \texttt{seed=42} \\
        \bottomrule
    \end{tabular}
\end{table}

\subsection{实验结果分析}

\subsubsection{总体性能对比 (RQ1)}
\label{subsubsec:overall_performance}

表~\ref{tab:overall_performance} 展示了各算法在三个数据集上的总体性能对比。其中 Hit@$K$ 反映前 $K$ 个推荐中是否命中至少一个真实正样本，NDCG@$K$ 进一步区分命中项在列表中的排位（排序质量）；二者同时上升，说明模型既更容易“猜中”用户兴趣，又能把更相关的物品排得更靠前。

\begin{table}[H]
    \centering
    \caption{各算法在三个数据集上的总体性能对比（Hit@10 与 NDCG@10）}
    \label{tab:overall_performance}
    \resizebox{0.94\linewidth}{!}{
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{lcccccc}
        \toprule
        \multirow{2}{*}{Method} & \multicolumn{2}{c}{MovieLens-1M} & \multicolumn{2}{c}{Yelp} & \multicolumn{2}{c}{ACM} \\
        \cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
        & Hit@10 & NDCG@10 & Hit@10 & NDCG@10 & Hit@10 & NDCG@10 \\
        \midrule
        FedGNN   & 0.1995 & 0.1062 & 0.0375 & 0.0218 & 0.2058 & 0.1198 \\
        FedHGNN  & 0.2135 & 0.1168 & 0.0448 & 0.0268 & 0.2432 & 0.1462 \\
        FedProto & 0.2188 & 0.1248 & 0.0482 & 0.0299$^*$ & 0.2456$^*$ & 0.1471$^*$ \\
        \midrule
        FedASCL (Ours) & 0.2196$^*$ & 0.1253$^*$ & 0.0487$^*$ & 0.0294 & 0.2443 & 0.1463 \\
        \bottomrule
    \end{tabular}}
    {\wuhao\songti 注：$^*$ 表示该数据集对应指标下的最优结果。}
\end{table}

各算法在 100 轮训练过程中的性能对比如图~\ref{fig:training_progress} 所示。各方法在 60--80 轮后大体进入平台区，但不同数据集上的收敛速度与后期波动并不一致：MovieLens-1M 上本文方法与强基线曲线最为接近，Yelp 上各方法绝对值整体偏低且曲线间距更小，ACM 上异构图对照基线在中后期 NDCG 一度略优于本文方法。上述现象与表~\ref{tab:overall_performance} 的局部领先格局相互印证，说明不存在单一方法在所有域、所有指标上统一占优的情况。

\begin{figure}[H]
    \centering
    \vspace{-0.4\baselineskip}
    \includegraphics[width=0.92\textwidth,height=0.26\textheight,keepaspectratio]{images/experiment.png}
    \vspace{-0.3\baselineskip}
    \caption{各算法训练过程性能对比（100轮训练）}
    \label{fig:training_progress}
\end{figure}

实验结果表明，在所列三种代表性基线设定下，FedASCL 与强基线在总体性能上处于同一量级：FedASCL 在 MovieLens-1M 与 Yelp 的 Hit@10 上略优，MovieLens-1M 的 NDCG@10 亦略优（约 0.4\%），Yelp 与 ACM 的 NDCG@10 略低于最优基线（约 0.5\%--1.7\%），ACM 上 Hit@10 亦略低（见表~\ref{tab:overall_performance}）。本文方法的优势并不体现在 IID 式全面领先，而主要体现在冷启动（表~\ref{tab:cold_start_res}，相对次优基线提升约 3\%--8\%，Hit@10 绝对增量最高约 0.0035）与极端 Non-IID（表~\ref{tab:non_iid_data}，$\alpha=0.1$ 时相对次优基线约 8.2\% 的 NDCG 提升）等困难子场景。

\subsubsection{冷启动场景性能评估 (RQ2)}
\label{subsubsec:cold_start}

为了公平比较，我们遵循\cite{zhang2024ifedrec}的暖用户和冷用户划分策略。对于每个数据集（MovieLens-1M、Yelp、ACM），我们根据用户的交互数量进行排序，选择交互数量最多的80\%的用户作为暖用户（Warm Users），用于训练模型；其余20\%的用户作为冷用户（Cold Users）。然后，我们从冷用户中随机抽取30\%的用户作为验证集，剩下的70\%的冷用户作为测试集。对于冷用户，我们仅保留其属性信息（如年龄、职业、研究兴趣等），移除所有历史交互记录，以模拟真实的冷启动场景。

与表~\ref{tab:overall_performance} 中的全量交互设定不同，冷启动实验对测试用户完全移除交互边，部分基线在零边条件下难以稳定训练或性能接近随机猜测，故冷启动表（表~\ref{tab:cold_start_res}）在总体四方法对照中进一步保留 FedHGNN、FedProto 与 FedASCL 三组可稳定运行的路线；FedHGNN 与 FedProto 分别代表联邦异构图结构建模与联邦原型对齐，便于观察本文双视图与原型联合设计在零交互测试用户上的作用，并仍在三个数据集上检查 FedASCL 的相对趋势。

针对系统中无交互记录的冷启动用户子集，各算法性能见表~\ref{tab:cold_start_res}。

\begin{table}[H]
    \centering
    \caption{冷启动用户场景下的性能对比（Hit@10 与 NDCG@10）}
    \label{tab:cold_start_res}
    \resizebox{\linewidth}{!}{
    \begin{tabular}{lcccccc}
        \toprule
        \multirow{2}{*}{Method} & \multicolumn{2}{c}{MovieLens-1M} & \multicolumn{2}{c}{Yelp} & \multicolumn{2}{c}{ACM} \\
        \cmidrule(lr){2-3}\cmidrule(lr){4-5}\cmidrule(lr){6-7}
        & Hit@10 & NDCG@10 & Hit@10 & NDCG@10 & Hit@10 & NDCG@10 \\
        \midrule
        FedHGNN & 0.0608 & 0.0352 & 0.0221 & 0.0122 & 0.0292 & 0.0160 \\
        FedProto & 0.0655 & 0.0402 & 0.0262 & 0.0147 & 0.0335 & 0.0184 \\
        FedASCL & 0.0690$^*$ & 0.0422$^*$ & 0.0284$^*$ & 0.0157$^*$ & 0.0347$^*$ & 0.0191$^*$ \\
        \bottomrule
    \end{tabular}}
    {\wuhao\songti 注：$^*$ 表示该数据集对应指标下的最优结果。}
\end{table}

由表~\ref{tab:cold_start_res} 可见，冷启动设定下各模型绝对值均显著低于表~\ref{tab:overall_performance}（MovieLens-1M 约为全量 Hit@10 的 31\%，Yelp 约为 58\%，ACM 约为 14\%），说明移除交互边后推荐难度陡增，属性增强只能部分补偿而非替代结构信号。FedASCL 相对次优基线的 Hit@10 提升为 ACM 约 3.6\%、MovieLens-1M 约 5.3\%、Yelp 约 8.4\%。NDCG@10 提升为 ACM 约 3.8\%、MovieLens-1M 约 5.0\%、Yelp 约 6.8\%。Hit@10 绝对增量在 MovieLens-1M 上最高（约 0.0035），ACM 因基线绝对值较低增量约 0.0012，呈现“稳定改善”而非“性能跃升”。Yelp 增益最高，与其丰富的用户与商户属性及极稀疏交互相一致。ACM 增益最低，因次优原型基线已能利用关键词与会议属性构建有效原型，且该域更依赖结构侧元路径。MovieLens-1M 居中。相对结构类对照基线，FedASCL 仍保持更大优势，但差距主要来自结构方法在零边测试用户上的退化，而非对次优基线的压倒性领先。

在冷启动场景下，基于结构的 GNN 对照基线在测试用户无交互边时性能严重下降。FedASCL 仍保持一定精度，主要得益于属性语义图、跨视图对齐与全局原型在测试期为无交互用户提供的排序信号；需说明训练在暖用户（80\%）交互图上进行，测试期对冷用户移除其交互边，与第五章在线推断采用同一 FedASCL 双视图流程（冷用户结构边为空）一致，离线指标以表~\ref{tab:cold_start_res} 为准。

\subsubsection{Non-IID 鲁棒性分析 (RQ3)}
\label{subsubsec:non_iid}

为了验证模型对数据异构的鲁棒性，我们调整 Dirichlet 参数 $\alpha$，观察模型在 MovieLens-1M 上的 NDCG@10 变化。

\begin{table}[H]
    \centering
    \caption{不同 Non-IID 程度 ($\alpha$) 下的性能鲁棒性对比（MovieLens-1M，NDCG@10）}
    \label{tab:non_iid_data}
    \begin{tabular}{lcccc}
        \toprule
        \multirow{2}{*}{Method} & \multicolumn{4}{c}{Dirichlet Parameter  $\alpha$} \\
        \cmidrule(lr){2-5}
         & $\alpha=0.1$ (Extreme) & $\alpha=0.5$ & $\alpha=1.0$ & $\alpha=\infty$ (IID) \\
        \midrule
        FedGNN   & 0.0825 (-26.9\%) & 0.0968 & 0.1062 & 0.1128 \\
        FedHGNN  & 0.0985 (-20.4\%) & 0.1108 & 0.1168 & 0.1238 \\
        FedProto & 0.1095 (-13.6\%) & 0.1188 & 0.1248 & 0.1267$^*$ \\
        FedASCL & 0.1185 (-5.4\%) & 0.1226 & 0.1253 & 0.1253 \\
        \bottomrule
    \end{tabular}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth,height=0.22\textheight,keepaspectratio]{images/non_iid_robustness.png}
    \caption[NonIID robustness curves]{不同数据异质性水平下的 NDCG@10 训练收敛对比四宫格子图，100 轮）}
    \label{fig:noniidplot}
\end{figure}

表~\ref{tab:non_iid_data} 汇总不同 $\alpha$ 下各算法最终指标，图~\ref{fig:noniidplot} 给出对应的 100 轮训练过程。

从图~\ref{fig:noniidplot} 可见，所有算法在极端异质性（$\alpha=0.1$）下性能均低于 IID 场景（$\alpha=\infty$），但随 $\alpha$ 增大并非严格等间距提升：部分对照基线在 $\alpha=0.5$ 至 $1.0$ 区间改善更明显，而部分异构图基线在 $\alpha=1.0$ 后已接近平台。FedASCL 曲线整体更平缓，在 $\alpha=0.5$ 起已略优于次优基线，但在 IID 端与最优基线基本持平且 NDCG@10 略低（0.1253 对 0.1267），说明双视图与原型对齐的主要收益来自强 Non-IID 而非 IID 上界。从 100 轮训练曲线观察，低 $\alpha$ 区间各方法中后期仍存在小幅波动，这与联邦场景下客户端采样随机性相一致。

表~\ref{tab:non_iid_data} 显示，当 $\alpha=0.1$ 时 FedASCL 相对 IID 的 NDCG@10 降幅约 5.4\%，低于次优基线的 13.6\% 与其他对照基线的 20.4\%。在极端 Non-IID 下，FedASCL 相对次优基线的 NDCG@10 提升约 8.2\%（0.1185 对 0.1095），相对其他对照基线约 20.3\%；该增益显著大于表~\ref{tab:overall_performance} 总体实验中二者在 NDCG 上的差距，说明双视图与全局原型的组合主要缓解的是异质性税而非 IID 上界。在默认 $\alpha=1.0$ 划分下（表~\ref{tab:overall_performance}），FedASCL 于 MovieLens-1M 的 NDCG@10 略优于强基线；而在 Non-IID 扫描的 IID 端（$\alpha=\infty$），FedProto 仍略高（0.1267 对 0.1253），说明二者在 IID 上界附近性能接近。

\subsubsection{消融实验}
\label{subsubsec:ablation}

为了量化各组件的贡献，我们对比了 FedASCL 的三个变体（见表~\ref{tab:ablation}）。

\begin{table}[H]
    \centering
    \caption{消融实验结果 (MovieLens-1M)}
    \label{tab:ablation}
    \begin{tabular}{lcc}
        \toprule
        Model Variant & Hit@10 & NDCG@10 \\
        \midrule
        w/o Attribute (移除属性视图) & 0.2065 & 0.1178 \\
        w/o CL (移除对比学习) & 0.2125 & 0.1205 \\
        w/o Proto (移除原型对齐) & 0.2152 & 0.1224 \\
        FedASCL (Full) & 0.2196 & 0.1253 \\
        \bottomrule
    \end{tabular}
\end{table}

消融实验在 MovieLens-1M 全量交互设定下进行（暖用户、$\alpha=1.0$），此时结构视图已较充分，故各模块相对贡献应小于冷启动或极端 Non-IID 场景。结果表明，移除属性视图后 Hit@10 下降约 6.0\%，NDCG@10 下降约 6.0\%。移除对比学习次之，Hit@10 下降约 3.2\%，NDCG@10 下降约 3.8\%。移除原型对齐再次之，Hit@10 下降约 2.0\%，NDCG@10 下降约 2.3\%。排序为属性 $>$ 对比学习 $>$ 原型，且 Hit 与 NDCG 的降幅并不完全对称，与模块设计分工一致：属性语义图提供内容信号，对比学习负责跨视图迁移，原型对齐主要缓解联邦异质性；在后一场景（表~\ref{tab:non_iid_data}）中其相对价值更大。三部分损伤叠加并非简单相加，表明模块间存在耦合。

\subsection{本章小结}
本章针对联邦推荐中的冷启动与 Non-IID 问题提出了 FedASCL 框架。主要工作包括在客户端基于 $k$NN 构建属性语义超图并与本地交互结构图构成双视图，分别经 HGNN 编码后在本地以 InfoNCE 做跨视图对齐，在服务器聚合各客户端语义原型并回传以约束本地表示，再结合元路径注意力完成推荐排序并与 BPR、对比及原型项联合优化。算法~\ref{alg:fedasclrc} 将上述环节组织为可实施的联邦训练过程，在常用稀疏实现下整体开销仍主要由交互边规模决定（见~\ref{subsec:fedascl_complexity}）。

在上述方法基础上，实验围绕总体性能、冷启动适应性与 Non-IID 鲁棒性展开。表~\ref{tab:overall_performance} 显示，FedASCL 与强基线在常规设定下各有优劣：Hit@10 在 MovieLens-1M 与 Yelp 上略优，ACM 上略低；NDCG@10 在 MovieLens-1M 上略优，Yelp 与 ACM 上略低于最优基线。表~\ref{tab:cold_start_res} 表明，冷启动下 FedASCL 相对次优基线的 Hit@10 与 NDCG@10 均提升约 3\%--8\%（Yelp 略高、ACM 略低），MovieLens-1M 上 Hit@10 绝对增量约 0.0035。表~\ref{tab:non_iid_data} 进一步显示，极端 Non-IID（$\alpha=0.1$）下 FedASCL 相对次优基线的 NDCG@10 提升约 8.2\%，FedASCL 的 IID 衰减（5.4\%）不足次优基线（13.6\%）的一半。结合表~\ref{tab:ablation} 的消融结果，跨视图对比与原型对齐在困难场景中承担互补分工。总体而言，FedASCL 在冷启动与 Non-IID 条件下相对次优基线取得稳定且可观的提升，常规全量设定下亦与强基线保持可比水平，表明所提双视图对比学习与全局原型对齐机制对联邦推荐中的语义缺失与客户端漂移具有切实改善效果。