# 第二章 图片补充建议

## 一、已有图片汇总

| 位置 | 文件 | 图题 | 说明 |
|------|------|------|------|
| 2.4 图神经网络基本原理 | 图神经网络.png | 图神经网络算法原理示意图 | ✓ 已有 |
| 2.4 同构图与异构图 | 同构图.png | 同构图示意图 | ✓ 已有 |
| 2.4 同构图与异构图 | 异构图.png | 异构图示意图 | ✓ 已有 |
| 2.4 元路径语义建模 | 元路径.png | 异构图与元路径示意图 | ✓ 已有 |
| 2.4 近邻图构建方法 | — | k-NN 近邻图构建流程（**算法伪代码**，非图） | ✓ 已有 |
| 2.5 联邦训练流程 | 联邦推荐图.jpg | 联邦学习架构示意图 | ✓ 已有 |
| 2.6 对比学习 | 对比学习.png | 对比学习示意图 | ✓ 已有 |

**无图的小节**：2.1 推荐任务建模、2.2 特征表示与预处理、2.3 推荐评价指标、2.4 超图建模、2.5 隐私保护机制、2.7 原型学习与语义对齐、2.8 模型压缩基础。

---

## 二、建议补充图片的位置与内容

### 1. 高优先级（强烈建议）

| 小节 | 建议图题 | 建议内容 | 理由 |
|------|----------|----------|------|
| **2.1 链接预测建模** | 用户-物品二部图与交互矩阵对应关系示意图 | 左侧：交互矩阵 $\mathbf{Y}$（用户×物品，0/1）；右侧：二部图（用户节点、物品节点、边）；用箭头或对应关系标明“矩阵非零元 ↔ 边”。 | 2.1 首次出现二部图与矩阵等价关系，后文 2.4 才出现图结构图；此处一图可统一“矩阵↔图”的直观，减少重复描述。 |
| **2.4 超图建模方法** | 超图与普通图对比示意图 | 上半：普通图（边只连 2 个节点）；下半：超图（一条超边连接多个节点，如一个节点+其 k 个近邻形成一条超边）；可标出关联矩阵 $\mathbf{H}$ 的示意。 | 正文有超边、关联矩阵和 HGCN 公式，但缺少“超边连多节点”的直观图，与普通图对比更易理解。 |
| **2.5 隐私保护机制** | 横向联邦与纵向联邦数据分布对比图 | 横向：多个客户端，每个有“同构特征、不同用户”（用表格或区块表示特征空间一致、样本不重叠）；纵向：多个参与方“同用户、异特征”（重叠用户 ID、不同特征列）。 | 横向/纵向仅文字描述容易混淆，一图对比“数据如何分布”可显著提升可读性。 |
| **2.7 原型学习与语义对齐** | 联邦原型聚合与语义对齐示意图 | 多客户端各自有“本地原型/嵌入”→ 上传 → 服务器“全局原型聚合”→ 下发；客户端用全局原型约束本地嵌入（箭头标“对齐”）；可标出“加噪上传”环节。 | 本地原型→聚合→全局原型→对齐的流程较抽象，图示便于与第三章方法对应。 |

### 2. 中优先级（建议有精力时补充）

| 小节 | 建议图题 | 建议内容 | 理由 |
|------|----------|----------|------|
| **2.2 特征表示与预处理** | 特征预处理与属性向量构建流程示意图 | 离散特征 → One-hot；连续特征 → Min-Max → 拼接成 $\mathbf{x}_v$；可选：一个简单示例（如 2 维离散+1 维连续→向量）。 | 为 2.4 超图/近邻图“基于 $\mathbf{x}_v$ 建图”做铺垫，一条流程更清晰。 |
| **2.6 对比学习**（在现有图基础上） | 若现有“对比学习.png”未体现双视图 | 属性视图（k-NN 超图）+ 结构视图（交互图/元路径）→ 同一节点两视图表示 → 正样本对 → 对比损失；与 FedASCL 双视图一致。 | 若现有图已是通用对比学习，可单独做“联邦推荐双视图对比”小图或在图注中说明与 2.6 文字对应。 |
| **2.8 语义感知压缩策略** | 语义感知压缩整体流程示意图 | 输入：多元路径/语义通道 → 重要性评估（如注意力权重）→ 动态选择保留通道 → 剪枝 + 残差梯度量化 → 压缩输出；可与第四章衔接。 | 2.8 概念多（语义重要性、动态选择、剪枝+量化），一图总览便于与第四章方法对照。 |

### 3. 低优先级（可选）

| 小节 | 建议图题 | 建议内容 | 理由 |
|------|----------|----------|------|
| 2.1 冷启动问题定义 | 三类冷启动问题对比图/表 | 用户冷启动 / 物品冷启动 / 系统冷启动 的对比（对象、难点、典型手段），可用简单表格或三栏图。 | 文字已分条列举，图可加快扫读，非必须。 |
| 2.2 相似度度量方法 | 余弦相似度几何示意图 | 二维或三维空间中两向量、夹角 $\theta$、$\cos\theta$ 与相似度关系。 | 公式已给出，图有助于几何直观。 |
| 2.5 隐私保护机制 | 差分隐私加噪示意图 | 查询结果 $f(D)$ → 加拉普拉斯噪声 → 输出分布；可选：$\varepsilon$ 与噪声规模的示意。 | 理解“加噪前后分布”时有帮助，但文字+公式已能说清。 |
| 2.8 结构化剪枝 / 量化 | 剪枝前后对比图 或 残差梯度量化流程图 | 剪枝：某层通道/核保留与剔除；量化：梯度+残差→量化→传输→本地误差。 | 若篇幅允许可增强 2.8 可读性，非必须。 |

---

## 三、实施建议

1. **先做高优先级 4 张**：二部图与矩阵对应、超图与普通图对比、横向/纵向联邦对比、联邦原型聚合与对齐。与后文第三、四章衔接最紧，且目前无图。
2. **文件名与引用**：新图可放在 `images/` 下，命名如 `bipartite_graph.png`、`hypergraph_compare.png`、`horizontal_vertical_fl.png`、`fed_prototype_align.png`，在 `chap2_related.tex` 中用 `\includegraphics` + `\caption` + `\label{fig:xxx}` 插入，正文用 `图~\ref{fig:xxx}` 引用。
3. **与现有图的关系**：2.1 若补充二部图，注意与 2.4 “异构图”中用户-物品二部为异构特例的说法一致；超图图可与 2.4 近邻图（k-NN 超边）文字呼应。
4. **2.3 推荐评价指标**：以公式和定义为主，一般不单独配图；若需要可做“Top-K 推荐与命中”的简单示意图，优先级低。

---

## 四、小结表

| 优先级 | 小节 | 建议图题 |
|--------|------|----------|
| 高 | 2.1 链接预测 | 用户-物品二部图与交互矩阵对应关系示意图 |
| 高 | 2.4 超图 | 超图与普通图对比示意图 |
| 高 | 2.5 隐私保护 | 横向联邦与纵向联邦数据分布对比图 |
| 高 | 2.7 原型学习 | 联邦原型聚合与语义对齐示意图 |
| 中 | 2.2 特征预处理 | 特征预处理与属性向量构建流程示意图 |
| 中 | 2.6 对比学习 | 视现有图情况补充双视图对比示意图 |
| 中 | 2.8 语义感知压缩 | 语义感知压缩整体流程示意图 |
| 低 | 2.1 冷启动 / 2.2 余弦 / 2.5 差分隐私 / 2.8 剪枝量化 | 见上表 |

如需，我可以按上述建议在 `chap2_related.tex` 中写好 `\begin{figure}...\end{figure}` 的占位与 \ref 引用，你只需替换为实际图片文件即可。
