# 论文检查报告

**检查时间**：2025-03-18  
**编译状态**：✓ 成功生成 PDF（90 页）

---

## 一、编译警告

| 类型 | 说明 |
|------|------|
| **未定义引用** | `fig:homo_hetero_graph`、`tab:exp_config_chap3`、`tab:exp_config_chap4` |
| **字体** | `\CJKttdefault` 未定义；部分字号替代 |
| **其他** | MiKTeX 更新提示、若干 Overfull hbox |

**建议**：多运行一次编译（`compile.ps1`）通常可消除未定义引用。

---

## 二、论文逻辑检查

### 2.1 结构
- 六章结构清晰：绪论 → 理论与技术 → FedASCL → 压缩策略 → 系统实现 → 总结
- 第一章 1.3 节与摘要、引言中工作概述一致

### 2.2 需关注点
1. **1.3 节引用**：首段“本文主要工作和贡献将在1节末详述”中的 `\ref{chap:intro}` 会显示为“1”，建议改为引用 1.3 节（可增加 `\label{sec:contrib}` 并改用 `\ref{sec:contrib}`）。
2. **同构/异构表述**：2.1 节提到“用户-物品二部图可视为异构图的特例”，与异构图定义（$|\mathcal{A}|>1$ 或 $|\mathcal{R}|>1$）一致。

---

## 三、参考文献问题

### 3.1 作者与 Cite Key 不一致（建议核对）

| Cite Key | 问题 |
|----------|------|
| `li2025fedcsr` | 第一作者为 Zhai（翟建阳），建议改为 `zhai2025fedcsr` |
| `wu2023fccf` | 第一作者为 Ji（纪嘉浩），建议改为 `ji2023fccf` |
| `zeng2025feddt` | 第一作者为 He（何子潇），建议改为 `he2025feddt` |
| `zhang2021hgsl` | 第一作者为 Zhao（赵佳楠），建议改为 `zhao2021hgsl` |
| `li2023metapath` | 第一作者为 王菲菲，建议改为 `wang2024metapath`（DATE 为 2024） |

### 3.2 其他问题

| Cite Key | 说明 |
|----------|------|
| `semfed2024` | DATE 为 2025，与 key 中 2024 不一致 |
| `guo2024fedgnn` | AUTHOR 含“其他”，建议补全作者列表 |
| `crad2024multiview` | AUTHOR 为“计算机研究与发展编辑部”，建议改为实际作者 |
| `wang2019ngcf` 与 `wang2019neural` | 疑为同一篇（Neural Graph Collaborative Filtering），建议检查是否重复 |

---

## 四、建议修改项

1. 再次运行 `compile.ps1`，确认未定义引用是否消失。
2. 视需要修正 bib 中的作者与 cite key（修正后需同步更新正文中的 `\cite{}`）。
3. 为首要贡献小节添加 `\label{sec:contrib}`，并将相关 `\ref{chap:intro}` 改为 `\ref{sec:contrib}`。
