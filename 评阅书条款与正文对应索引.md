# 评阅书条款 ↔ 正文对应索引

> **用途**：查「某条评阅意见在论文里落在哪」。**页码以每次编译后的 PDF 目录为准**（随增删表浮动）；下表以 **`chapters/*.tex` 节标题 / 标签** 定位，便于检索。  
> **PDF 输出路径**：`output/伍勋高-202333248.pdf`（由 `./compile.sh` 生成）。

---

## 《论文评阅书-1》（同意修改后答辩）

| 条款 | 评阅意见（摘要） | 正文对应内容 | 主要检索位置 |
|:---:|:---|:---|:---|
| **1** | 自身工作与已有构件区分不清 | 「问题说明」末段：沿用构件、联邦组织方式、与第四章压缩衔接；创新在组合与流程而非单点重写算子 | `chapters/chap3_method.tex` — `\subsection{问题说明}`，自「需要说明的是，…」起（约第 18 行一段） |
| **2** | 图需在正文中有说明 | 总架构图、跨视图、原型、属性语义图、Non-IID 等处图前导读；Non-IID 处说明表—曲线同页读法 | `chap3_method.tex` — `fig:fedascl_framework`（图前段）、`fig:attr_semantic_build`、`fig:cross_view_cl`、`fig:prototype_alignment`、`fig:non_iid_robustness` 等 |
| **3** | 3.7 算法流程偏薄 | 「流程与符号对应关系」子节 + 伪代码后与复杂度衔接 | `chap3_method.tex` — `\subsubsection{流程与符号对应关系}`；`\subsection{算法流程}` 内 `alg:fedasclrc` 及紧随阐释段落 |
| **4** | 建议增加 2025 年方法 | **不补主表数值**：「对比基线模型」两段——FELLRec 等；FedPCL、FRCSU、FedDMR 等与管线/协议不完全同构故不并入 `tab:overall_performance` | `chap3_method.tex` — `\subsubsection{对比基线模型}`（紧接「参数设置」之前） |
| **5** | 第三、四章小结先写工作再写效果 | 小结首段先方法链条 / 上传链路，再归纳实验 | `chap3_method.tex` — `\subsection{本章小结}`；`chap4_compression.tex` — `\subsection{本章小结}` |
| **6** | 总体架构与技术栈混排，建议增技术架构图 | 逻辑分层 `fig:sys_arch` + 实现栈 `fig:sys_tech_flow` + `tab:tech_stack`，需求分析处双图互指 | `chap5_system.tex` — `\subsection{系统需求分析}`、`\subsection{系统总体架构设计}`（图环境带 `fig:sys_arch`、`fig:sys_tech_flow`） |

---

## 《论文评阅书-2》（同意答辩）

| 条款 | 评阅意见（摘要） | 正文对应内容 | 主要检索位置 |
|:---:|:---|:---|:---|
| **1** | 表 3/表 4 基线不一致；第四章缺 ACM 且无说明 | 冷启动基线取舍；总览表 `tab:chap3_exp_table_guide`；第四章数据集段写明不并列 ACM 压缩、与第三章划分差异；`tab:exp_config_chap4` 与第三章表互指 | `chap3_method.tex` — `tab:chap3_exp_table_guide`、`tab:cold_start_res` 节前说明；`chap4_compression.tex` — 数据集与预处理长段（含 ACM 不并列理由）、`tab:exp_config_chap4` 后互指句 |
| **2** | 1.2 扩充大模型与图对比学习对照 | 冷启动综述后的「问题设定对照」段：优化目标、表征形态、主实验不并列大模型基线 | `chap1_intro.tex` — `\subsubsection{联邦推荐系统冷启动研究现状}`，「在归纳上述若干代表性工作之后，…」起 |
| **3** | 硬阈值剪枝欠灵活；原型加权在极端倾斜下不足 | 全局原型加权公式后**极端倾斜局限**及可能变体；第四章小结末 **$\rho,b$、Top-$K$** 与自适应/稳健聚合展望 | `chap3_method.tex` — `\subsubsection{全局原型聚合}` 公式后段落（约第 177 行）；`chap4_compression.tex` — `\subsection{本章小结}` 末段 |
| **4** | 3.9.1 应列出真实属性 | `tab:dataset_attr`；数据集节首句显式指向该表 | `chap3_method.tex` — `\subsubsection{数据集与预处理}` 首句；表 `tab:dataset_attr` |

---

## 与评阅相关、但不写入正文正文的材料

| 材料 | 作用 |
|:---|:---|
| `评阅意见与正文修改对照.md` | 修改过程与答辩备忘；**不编入** `main.tex` |
| `评阅书意见与作者回应说明.md` | 逐条「不宜写入正文」的备忘 + 答辩短句 |
| `论文评阅修改计划.md` | 执行清单与自评 |

---

## 重新编译

在项目根目录执行：

```bash
./compile.sh
```

生成 PDF：**`output/伍勋高-202333248.pdf`**。

---

*本索引随 `chapters/*.tex` 维护；若章节增删导致印刷页码变化，以 PDF 内目录为准。*
