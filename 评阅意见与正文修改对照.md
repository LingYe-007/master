# 评阅意见与正文修改对照

> **前提**：不在学位论文中**补充新的数值实验**（不新增 ACM 压缩主表、不与 FELLRec 等做完全同管线 HR/NDCG 并排）；回应策略为：**正文论证写满、表与互指写清、局限与后续工作写诚实**，使「说明原因」与「控制变量」在学术上可辩护。  
> **说明**：本文件为**修改对照与答辩备忘**，不进入 `main.tex` 编译；文中亦**不出现**「为回应评阅」一类元叙述。与评阅意见的**逐条口头回应与不宜写入正文之备忘**另见同目录 **`评阅书意见与作者回应说明.md`**。

---

## 一、《论文评阅书-1》（同意修改后答辩）— 意见与正文落点

| 序号 | 评阅意见（摘要） | 正文中的对应修改 | 主要文件与位置（便于检索） |
|:----:|------------------|------------------|---------------------------|
| 1 | 属性语义图、跨视图对比、语义级注意力等多处引用已有工作，**自身工作不突出** | 在提出 FedASCL 之后增加一整段：**沿用哪些构件、在联邦下的目标/对象/与第四章压缩如何衔接**；明确「创新在组合与联邦设定下的训练目标与流程，而非单点重写算子」 | `chapters/chap3_method.tex` — `\subsection{问题说明}` 末段（「需要说明的是，…」起） |
| 2 | **所有图**在正文中要有对应描述（例：图 12） | 总架构图前补充**模块与数据流**导读；跨视图、原型对齐图前各补**导读句**；属性语义图前**保留**「图…与上文形式化描述对应」；Non-IID 浮动体前说明**上表下曲线**读法 | `chapters/chap3_method.tex` — `fig:fedascl_framework`、`fig:cross_view_cl`、`fig:prototype_alignment`、`fig:attr_semantic_build`、`fig:non_iid_robustness` 等处 |
| 3 | **3.7 算法流程**仅伪代码，作为一节偏薄 | 增设 `\subsubsection{流程与符号对应关系}`（`cnenumerate` 四阶段）；算法后增加**执行顺序与符号—伪代码步骤**对应说明，并接到复杂度节 | `chapters/chap3_method.tex` — `\subsection{算法流程}` |
| 4 | 第三章实验建议**增加 2025 年方法** | **不补跑**：在「对比基线」段以文字说明 FELLRec 等及 FedPCL、FRCSU、FedDMR 等与主表基线**不并入**的理由（**两段**承接原定性表信息密度）；**原定性表** `tab:baseline_scope_2025` 已删除 | `chapters/chap3_method.tex` — `\subsubsection{对比基线模型}` |
| 5 | **第三、四章小结**宜先写**所做工作**再写效果 | 第三章小结首段写清**方法链条**再写实验；第四章小结首句写清**上传链路两类机制与评测方式**再归纳 RQ | `chapters/chap3_method.tex`、`chapters/chap4_compression.tex` — `\subsection{本章小结}` |
| 6 | **图 20** 总体架构与技术架构混排；建议增技术架构图 | **不强制 drawio**：保留逻辑分层图 `fig:sys_arch`；新增实现栈示意图 `fig:sys_tech_flow`（与 `tab:tech_stack` 对应）；需求分析处**双图互指** | `chapters/chap5_system.tex` — `\subsection{系统总体架构设计}`、`\subsection{系统需求分析}` |

**评阅书-1 第 6 条「另增技术架构图」的回应边界**：以 **LaTeX 结构化示意图**满足「技术实现视角」与逻辑图分离；若学院模板**强制位图**，可仅替换 `fig:sys_tech_flow` 的呈现形式，**不必**改论证结构。

---

## 二、《论文评阅书-2》（同意答辩）— 意见与正文落点

| 序号 | 评阅意见（摘要） | 正文中的对应修改 | 主要文件与位置 |
|:----:|------------------|------------------|----------------|
| 1 | 表 3（五基线）与表 4（两基线）不一致；第四章仅 ML、Yelp、**缺 ACM** 且缺解释 | **表 4**：冷启动节前已有**基线收窄**说明（零边设定）。**两章衔接**：新增总表 `tab:chap3_exp_table_guide`，集中写清总体表 / 冷启动表 / 与第四章差异；第四章 `tab:exp_config_chap4` 后与第三章该表**互指**。**ACM**：第四章数据集段写明**不并列 ACM 压缩数值**的原因（双变量混淆），并补一句强调**与「控制变量」写法一致**；**不补** ACM 压缩主文表 | `chapters/chap3_method.tex`（`tab:chap3_exp_table_guide`、冷启动段）、`chapters/chap4_compression.tex`（数据集与预处理、`tab:exp_config_chap4` 后段） |
| 2 | **1.2 节**适当扩充大模型驱动推荐与本文图对比学习的对比 | 在**冷启动综述**已写 FELLRec、大模型依赖等之后，增加**问题设定对照段**（优化目标、消息传递/正负对、联邦可交换表征、主实验不并列大模型基线） | `chapters/chap1_intro.tex` — `\subsubsection{联邦推荐系统冷启动研究现状}`（「在归纳上述若干代表性工作之后…」段） |
| 3 | **硬阈值剪枝**欠灵活；**全局原型简单加权**在极端数据下鲁棒性不足 | 原型聚合公式后补充**样本量加权在极端倾斜时的局限**及可能改进方向；第四章小结末写清 **$\rho,b$ 硬阈值、Top-$K$ 敏感性**及**自适应剪枝 / 稳健聚合**后续方向（不声称本文已实现） | `chapters/chap3_method.tex`（全局原型聚合）、`chapters/chap4_compression.tex`（本章小结末） |
| 4 | 3.9.1 介绍数据集时**未列出真实属性** | 已有 `tab:dataset_attr`；在 **3.9.1 数据集与预处理**首句**显式指向**该表，与 3.3.1 的 $\mathbf{x}_v$ 叙述闭环 | `chapters/chap3_method.tex` — `\subsubsection{数据集与预处理}` |

**评阅书-2 第 1 条「补充 ACM 结果」的回应边界**：评阅原文为「**补充 ACM 实验结果或在文中明确说明剔除原因**」——在**不补实验**前提下，正文采取**后者**：理由写进第四章数据集段 + 第三章总表第三行 + 互指，使读者**不会**把「第三章三数据集主结果」与「第四章两数据集压缩」误读为同一实验轴。

---

## 三、其它必要修正（非评阅条文，但影响正确性）

| 事项 | 处理 |
|------|------|
| 绪论研究背景段误插入 `\\]`、孤立反斜杠等 | 已清理，避免无效数学环境与版式风险 | `chapters/chap1_intro.tex` |
| 参考文献 / 交叉引用警告 | 以本机 `output/main.log` 为准逐项核对（编译脚本对 latexmk 输出有过滤） | 运行 `./compile.sh` 后自查 |

---

## 四、主要修改文件一览（按章节）

| 文件 | 与评阅的关联（概括） |
|------|----------------------|
| `chapters/chap1_intro.tex` | 1.2 冷启动综述后的大模型—图路线对照；背景段 LaTeX 修复 |
| `chapters/chap3_method.tex` | 贡献边界、图导读、算法子节、数据集与属性表、基线与 2025 路线文字说明、冷启动说明、实验总表 `tab:chap3_exp_table_guide`、原型局限、小结 |
| `chapters/chap4_compression.tex` | ACM 与划分差异说明（含**不写主文 ACM 压缩表**的可辩护理由）、配置表互指、小结局限与展望 |
| `chapters/chap5_system.tex` | 逻辑架构图 + 技术栈示意图 + 需求分析互指 |
| `论文评阅修改计划.md` | 执行清单与自评表（**过程文档**，不编入论文 PDF） |

---

## 五、不补实验前提下，答辩时可用的「一句话辩护」（备忘）

- **2025 / 大模型基线不进主表**：管线、输入形态、算力与指标口径与本文异构图联邦 HR/NDCG **不对齐**；绪论与第三章「对比基线模型」子节已界定边界（**原定性对照表已删除**，以文字承接）。  
- **ACM 压缩不进主表**：第三章 ACM 为 $N{=}50,\alpha{=}1.0$；第四章刻意 $N{=}100,\alpha{=}0.5$；并列 ACM 会**混淆**「压缩」与「重划分」；若要给 ACM 压缩数，须**先固定划分再只动压缩**（属新实验，本文从略并在文中写明）。

---

*文档版本：与当前仓库 `chapters/*.tex` 修改同步；若你后续只改正文不改本对照表，请以 Git 差异为准。*
